services:
  view-counter:
    build: ./backend
    container_name: okal-api
    env_file:
      - .env
    volumes:
      - ./backend/data:/root/data
    restart: always
    networks:
      - web_network
networks:
  web_network:
    external: true
